[project]
name = "touchview"
version = "0.2.0"
description = "Wayland overlays for visualizing touchpad/touchscreen gestures and keyboard shortcuts"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Klim Kostiuk" }]
keywords = ["wayland", "touchpad", "touchscreen", "overlay", "keyboard", "gestures", "layer-shell"]
classifiers = [
    "Environment :: Other Environment",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Topic :: Desktop Environment",
]
dependencies = [
    "evdev>=1.9.3",
    "pycairo>=1.27",
    "PyGObject>=3.50",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["touchview.py", "keyview.py", "_overlay.py"]

[project.scripts]
touchview = "touchview:main"
keyview = "keyview:main"
